function setup() {
  createCanvas(600, 400);
  background(255);
  noLoop();
}

function draw() {
  // ------------------------
  // 👕 해군 군복 몸통 (네이비 + 흰색 디테일)
  // ------------------------
  fill(10, 30, 70); // 네이비 진한색
  stroke(0);
  strokeWeight(2);
  beginShape();
  vertex(250, 270);
  bezierVertex(260, 245, 275, 235, 300, 235);
  bezierVertex(325, 235, 340, 245, 350, 270);
  vertex(350, 320);
  vertex(250, 320);
  endShape(CLOSE);

  // 소매
  ellipse(250, 265, 20, 30);
  ellipse(350, 265, 20, 30);

  // ------------------------
  // 💪 팔 (피부색 + 윤곽선)
  // ------------------------
  fill(255, 220, 180);
  ellipse(250, 295, 15, 45);
  ellipse(350, 295, 15, 45);

  // ------------------------
  // 🧣 목 (피부색 + 윤곽선)
  // ------------------------
  fill(255, 220, 180);
  rect(285, 220, 30, 20, 10);
  noFill();
  stroke(0);
  strokeWeight(2);
  rect(285, 220, 30, 20, 10);

  // ------------------------
  // 😀 얼굴 (기본)
  // ------------------------
  stroke(0);
  strokeWeight(2);
  fill(255, 220, 180);
  ellipse(300, 180, 140, 140);

  // 귀 (윤곽선 추가)
  fill(255, 220, 180);
  stroke(0);
  strokeWeight(2);
  ellipse(230, 180, 20, 35);
  ellipse(370, 180, 20, 35);

  // 머리
  fill(40, 30, 20);
  noStroke();
  arc(300, 145, 140, 110, PI, 0, CHORD);

  // 눈
  fill(255);
  stroke(0);
  strokeWeight(1.5);
  ellipse(270, 180, 35, 35);
  ellipse(330, 180, 35, 35);

  // 눈동자
  fill(0);
  noStroke();
  ellipse(270, 180, 24, 24);
  ellipse(330, 180, 24, 24);

  // 하이라이트
  fill(255);
  ellipse(266, 175, 5, 5);
  ellipse(326, 175, 5, 5);

  // 눈썹
  stroke(0);
  strokeWeight(4);
  line(250, 160, 280, 160);
  line(320, 160, 350, 160);

  // 코
  stroke(80);
  strokeWeight(2);
  line(300, 175, 300, 190);

  // 입
  stroke(150, 0, 0);
  strokeWeight(2.5);
  noFill();
  arc(300, 205, 50, 20, 0, PI);

  // 얼굴 윤곽
  noFill();
  stroke(0);
  strokeWeight(1.5);
  arc(300, 180, 140, 140, 0.1 * PI, 0.9 * PI);

  // ------------------------
  // 해군 군복 디테일 추가
  // ------------------------

  // 단추 라인 (중앙)
  stroke(255);
  strokeWeight(4);
  for(let y = 250; y <= 300; y += 15){
    point(300, y);
  }

}
